# Assignment 5a SQL

# Task 1: Total price of each of the first 5 orders, ordered by order_id
SELECT o.order_id, SUM(p.price * li.quantity) AS total_price
FROM orders o
JOIN line_items li ON o.order_id = li.order_id
JOIN products p ON li.product_id = p.product_id
GROUP BY o.order_id
ORDER BY o.order_id
LIMIT 5;

# Task 2: For each customer, average total price of their orders (subquery)
SELECT c.customer_name, AVG(t.total_price) AS average_order_price
FROM customers c
JOIN (
  SELECT o.customer_id, o.order_id, SUM(p.price * li.quantity) AS total_price
  FROM orders o
  JOIN line_items li ON o.order_id = li.order_id
  JOIN products p ON li.product_id = p.product_id
  GROUP BY o.customer_id, o.order_id
) AS t ON c.customer_id = t.customer_id
GROUP BY c.customer_id, c.customer_name
ORDER BY c.customer_name;

# Task 3: Creating a new order (Perez and Sons, employee Miranda Harris, 10 of 5 least expensive products)
SELECT customer_id FROM customers WHERE customer_name = 'Perez and Sons';
SELECT employee_id FROM employees WHERE first_name = 'Miranda' AND last_name = 'Harris';
SELECT product_id FROM products ORDER BY price ASC LIMIT 5;
BEGIN;
INSERT INTO orders (customer_id, employee_id, date) VALUES (16, 7, '2026-05-01') RETURNING order_id;
INSERT INTO line_items (order_id, product_id, quantity)
VALUES (999, 23, 10), (999, 17, 10), (999, 13, 10), (999, 5, 10), (999, 11, 10);
COMMIT;
SELECT * FROM line_items WHERE order_id = 999;

# Task 4: Employees associated with more than five orders
SELECT e.first_name, e.last_name, COUNT(o.order_id) AS order_count
FROM employees e
JOIN orders o ON e.employee_id = o.employee_id
GROUP BY e.employee_id, e.first_name, e.last_name
HAVING COUNT(o.order_id) > 5
ORDER BY e.last_name;

